nav(class="navbar navbar-expand navbar-dark bg-dark static-top")
    a(class="navbar-brand mr-1" href="accueil") Wake Me Up
    button(class="btn btn-link btn-sm text-white order-1 order-sm-0" id="sidebarToggle" href="#")
        i(class="fas fa-bars")
    div(id="ifConnected" class="ml-auto")
        .btn-group(class="text-white")
            p= lastName
            p -
            p= firstName
        .btn-group(class="text-white small")
            a( class="btn btn-link text-white " onclick="disconnect();") Deconnexion

    